{
  "name": "kafka-consumer-worker",
  "version": "1.0.0",
  "description": "Generic Kafka consumer worker — consume จาก topic แล้ว forward ไป downstream URL",
  "type": "module",
  "scripts": {
    "start": "node --import tsx src/index.ts",
    "build": "tsc",
    "generate:helm": "npx tsx scripts/generate-helm-values.ts",
    "deploy": "npm run generate:helm && helm upgrade --install kafka-workers ./helm/kafka-worker -f helm/kafka-worker/values-generated.json"
  },
  "dependencies": {
    "kafkajs": "2.2.4"
  },
  "devDependencies": {
    "typescript": "5.5.4",
    "tsx": "4.16.2",
    "@types/node": "20.14.11"
  }
}
